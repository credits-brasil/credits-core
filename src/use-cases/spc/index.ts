import { spc11UseCase } from "./spc-11.use-case";
import { spc240UseCase } from "./spc-240.use-case";
import { spc246UseCase } from "./spc-246.use-case";
import { spc257UseCase } from "./spc-257.use-case";
import { spc323UseCase } from "./spc-323.use-case";
import { spc325UseCase } from "./spc-325.use-case";
import { spc337UseCase } from "./spc-337.use-case";
import { spc454UseCase } from "./spc-454.use-case";
import { spc515UseCase } from "./spc-515.use-case";
import { spc626UseCase } from "./spc-626.use-case";
import { spc627UseCase } from "./spc-627.use-case";
import { spc628UseCase } from "./spc-628.use-case";
import { spc629UseCase } from "./spc-629.use-case";
import { spc630UseCase } from "./spc-630.use-case";
import { spc631UseCase } from "./spc-631.use-case";
import { spc632UseCase } from "./spc-632.use-case";
import { spc633UseCase } from "./spc-633.use-case";
import { spc668UseCase } from "./spc-668.use-case";
import { spc674UseCase } from "./spc-674.use-case";
import { spc675UseCase } from "./spc-675.use-case";
import { spc676UseCase } from "./spc-676.use-case";
import { spc677UseCase } from "./spc-677.use-case";
import { spc678UseCase } from "./spc-678.use-case";
import { spc695UseCase } from "./spc-695.use-case";
import { spc940UseCase } from "./spc-940.use-case";

export class SPCUseCases {
  async spc11(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc11UseCase(document, typeDocument, insumos);
  }

  async spc240(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc240UseCase(document, typeDocument, insumos);
  }

  async spc246(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc246UseCase(document, typeDocument, insumos);
  }

  async spc257(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc257UseCase(document, typeDocument, insumos);
  }

  async spc323(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc323UseCase(document, typeDocument, insumos);
  }

  async spc325(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc325UseCase(document, typeDocument, insumos);
  }

  async spc337(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc337UseCase(document, typeDocument, insumos);
  }

  async spc454(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc454UseCase(document, typeDocument, insumos);
  }

  async spc515(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc515UseCase(document, typeDocument, insumos);
  }

  async spc626(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc626UseCase(document, typeDocument, insumos);
  }

  async spc627(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc627UseCase(document, typeDocument, insumos);
  }

  async spc628(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc628UseCase(document, typeDocument, insumos);
  }

  async spc629(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc629UseCase(document, typeDocument, insumos);
  }

  async spc630(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc630UseCase(document, typeDocument, insumos);
  }

  async spc631(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc631UseCase(document, typeDocument, insumos);
  }

  async spc632(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc632UseCase(document, typeDocument, insumos);
  }

  async spc633(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc633UseCase(document, typeDocument, insumos);
  }

  async spc668(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc668UseCase(document, typeDocument, insumos);
  }

  async spc674(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc674UseCase(document, typeDocument, insumos);
  }

  async spc675(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc675UseCase(document, typeDocument, insumos);
  }

  async spc676(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc676UseCase(document, typeDocument, insumos);
  }

  async spc677(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc677UseCase(document, typeDocument, insumos);
  }

  async spc678(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc678UseCase(document, typeDocument, insumos);
  }

  async spc695(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc695UseCase(document, typeDocument, insumos);
  }

  async spc940(
    document: string,
    typeDocument: "CPF" | "CNPJ",
    insumos: number[],
  ) {
    return spc940UseCase(document, typeDocument, insumos);
  }
}
